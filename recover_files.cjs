const fs = require(" fs\); const path = require(\path\); const lines = fs.readFileSync(\C:/Users/Antz Work.Antz/.gemini/antigravity-ide/brain/bc279b18-b029-4ddb-89e1-d6960fca24fa/.system_generated/logs/transcript_full.jsonl\, \utf-8\).split(\\\n\); for(const line of lines){ if(!line.trim()) continue; const obj = JSON.parse(line); if(obj.tool_calls){ for(const tc of obj.tool_calls){ if(tc.name === \write_to_file\ && tc.args && tc.args.TargetFile && tc.args.CodeContent){ const fpath = tc.args.TargetFile; if(fpath.includes(\src/MVVM\)){ fs.mkdirSync(path.dirname(fpath), {recursive: true}); fs.writeFileSync(fpath, tc.args.CodeContent); console.log(\Recovered \ + fpath); } } } } } 
